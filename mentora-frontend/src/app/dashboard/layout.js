export const metadata = {
  title: "Dashboard",
  description: "Track intro emails, meetings and satisfaction across your mentor matches.",
};

export default function DashboardLayout({ children }) {
  return children;
}
