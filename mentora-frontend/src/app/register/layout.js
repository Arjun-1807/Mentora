export const metadata = {
  title: "Create an account",
  description: "Join Mentora as a startup founder or as a mentor.",
};

export default function RegisterLayout({ children }) {
  return children;
}
