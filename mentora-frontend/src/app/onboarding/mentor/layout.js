export const metadata = {
  title: "Mentor onboarding",
  description: "Set up your Mentora mentor profile so startups can find you.",
};

export default function MentorOnboardingLayout({ children }) {
  return children;
}
