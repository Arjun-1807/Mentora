export const metadata = {
  title: "Mentor Dashboard",
  description: "Review incoming startup match requests and track your mentoring activity.",
};

export default function MentorDashboardLayout({ children }) {
  return children;
}
