export const metadata = {
  title: "Upload pitch deck",
  description: "Upload your pitch deck and Mentora extracts a structured startup profile from it.",
};

export default function UploadLayout({ children }) {
  return children;
}
