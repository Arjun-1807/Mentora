export const metadata = {
  title: "Mentor matches",
  description: "Your top mentor matches, ranked by fit with your startup profile.",
};

export default function MatchesLayout({ children }) {
  return children;
}
